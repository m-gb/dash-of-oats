import { CutDescriptionPipe } from './cut-description.pipe';

describe('CutDescriptionPipe', () => {
  it('create an instance', () => {
    const pipe = new CutDescriptionPipe();
    expect(pipe).toBeTruthy();
  });
});
